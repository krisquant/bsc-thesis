# Run Tracker App Backend

## Installation

1. Install dependencies:
```bash
uv sync
```

2. Copy environment file:
```bash
cp .env.sample .env
```

3. Edit `.env` with your configuration values.

## Docker Build & Run

Build and run with Docker Compose:
```bash
docker-compose up --build
```
